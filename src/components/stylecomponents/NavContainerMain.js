import styled from "styled-components";

const NavContainerMain = styled.div`
  background-color: ${props => props.bgc || "transparent"};
  width: 100%;
  margin: 0 auto;
`;
export default NavContainerMain;
